"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.WeekOfYearLimit = void 0;
const gotime_parts_1 = require("../enums/gotime_parts");
const gotime_unts_1 = require("../enums/gotime_unts");
const _limit_1 = require("./_limit");
class WeekOfYearLimit extends _limit_1.GoTimeLimit {
    constructor() {
        super(...arguments);
        this.part = gotime_parts_1.GoTimePart.weekOfYear;
    }
    getCurrentValue(date) {
        const start = new Date(date.getFullYear(), 0, 1);
        return Math.ceil(((date.getTime() - start.getTime()) / 86400000 + start.getDay() + 1) / 7);
    }
    computeUnit() {
        return gotime_unts_1.GoTimeUnit.week;
    }
    toNumber(n) {
        const result = super.toNumber(n);
        if (isNaN(result) || result < 1 || result > 52)
            throw new Error(`Invalid week of year ${n}`);
        return result;
    }
}
exports.WeekOfYearLimit = WeekOfYearLimit;
